import Footer from "./footer";
import Header from "./header";
import Tooltip from "./tooltip";

const Components = {
  Footer,
  Header,
  Tooltip,
};

export default Components;

export { Footer, Header, Tooltip };
