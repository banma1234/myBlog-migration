import commentHandler from "./commentHandler";
import mdParser from "./mdParser";

const Components = {
  commentHandler,
  mdParser,
};

export default Components;

export { commentHandler, mdParser };
