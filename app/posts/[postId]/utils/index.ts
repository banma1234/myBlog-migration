import getPost from "./getPost";
import commentHandler from "./commentHandler";
import mdParser from "./mdParser";

const Components = {
  getPost,
  commentHandler,
  mdParser,
};

export default Components;

export { getPost, commentHandler, mdParser };
