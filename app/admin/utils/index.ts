import getAllPosts from "app/search/getAllPosts";
import getSeriesInfo from "./getSeriesInfo";

const Components = {
  getAllPosts,
  getSeriesInfo,
};

export default Components;

export { getAllPosts, getSeriesInfo };
