import getIndexBoard from "./getIndexBoard";
import getRecommendPost from "./getRecommendPost";
import globalMetaData from "./globalMetaData";
import GLOBAL_SCRIPT from "./globalScript";

const Components = {
  getIndexBoard,
  getRecommendPost,
  globalMetaData,
  GLOBAL_SCRIPT,
};

export default Components;

export { getIndexBoard, getRecommendPost, globalMetaData, GLOBAL_SCRIPT };
